apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: vote-service
  name: vote-service
spec:
  ports:
    - name: "vote-port"
      port: 5001
      targetPort: 8080
  selector:
    io.kompose.service: vote-service
